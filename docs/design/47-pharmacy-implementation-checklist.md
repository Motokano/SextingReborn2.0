# Sol：新版制药系统实施清单

状态：2026-09-10 已部署并完成构建、运行时回归与本地页面验收。复选框表示本轮实现状态；暂缓内容仍明确保留在数据与交付说明中。

## 接手顺序与边界

先读[当前主文档](47-pharmacy-system.md)，再读[结构规划](47-pharmacy-full-recipe-plan.json)。专题只补细节，旧主文档归档不参与决策。用户要求正常运行即可，不做旧存档迁移，不建配药历史，不扩建整套流血/濒死系统。使用PowerShell 7与UTF-8写文件，检查并保护工作区已有修改。

“部署”在本交接中指代码与数据落地、构建、测试及本地游戏验收，不自动授权外网发布或生产服务器操作。

## A. 数据收口与禁用分支

- [x] 审核现有57条配方及22条新增候选，按输出实际消费者筛选，不盲目将79条全部启用。三条兽心替代线明确储备；止血与复苏相关专用分支无消费者则暂缓。
- [x] 新增9件口服成品（5种功能丸、3种镇痛丸、清毒丸）及2件鹿茸中间粉按实际可用性落源CSV；镇痛原ID更名苦根镇痛丸。其余8种加上清毒共9种新增口服，数量以源清单核对。
- [x] 两条原配方调整：苦根镇痛丸增加丸基；活络药膏原苦根粉改红花粉。后者用料更换需同步有效成分归属，不能沿旧镇痛成分结算。
- [x] 退役11件固定注射成品：检查所有引用后清理活跃数据，无旧库存兼容；动态potion_compound_injection保留。
- [x] 药粉成分与固定成品配方投入分开建模：糖水不提供额外恢复或依赖，新增药不得复制普通丸旧饱食字段。从真实配方推导活性组成或记录统一组成字段，避免两处手填数值失配。
- [x] 保留原粉已有配药参数；清毒额外代谢独立字段，不把负毒性直接用于口服排毒。精炼护肝粉负毒性绝对值下降的旧问题不借本轮擅自重平衡。
- [x] 整理每项生成入口；构建器支持新增字段、禁用标记和配方级成本，避免结构草案字段被静默忽略。

涉及：data/items/pharmacy_base.csv、data/pharmacy-recipes.json、data/pharmacy-buff-matrix.json、data/pharmacy-system-config.csv、tools/build-items-json.mjs、tools/build-pharmacy-recipes.mjs、tools/build-pharmacy-buffs.mjs。

## B. 制作费用、推荐等级、经验

- [x] 将制药工艺/配方等级硬阻拦替换为推荐等级；只影响life_pharmacy，不取消其他技能门槛。
- [x] 有效推荐等级=max(工艺,配方)，不足惩罚每级0.05。保留每级0.005加成，中性下限0.15；心情通过既有唯一入口修正，显示值与实际掷骰一致，满级既有行为对照保留。
- [x] 支持proposed_cost映射到实际每配方费用；不全局修改压片工艺以致全部配方涨价。四镇痛丸燃料15/20/30/40、ticks2/3/4/5；续力/幻梦丸25与4，其余三功能丸20与3，清毒20与3；体力均2。
- [x] 成功标准产物；普通失败药渣。增加25%失败经验并保持精度，成功批次只发一次；普通配方已学状态保持，配置历史移除。
- [x] 经验单位可采用四倍固定点，但读取曲线、上限、展示必须一致换算；不得只改写入仍parseInt读取导致丢失。

涉及：js/pharmacy-station.js（现有成功经验与最终随机判定）、js/station-craft-core.js、js/recipe-system.js、js/recipe-schema.js、js/pharmacy-station-panel.js及构建器。先查共享逻辑调用范围再改。

## C. 配置与失败出口

- [x] resolve/assess拆清：无效输入不开工、实际配伍失败、正常配置三种结果；预览过滤不能改变权威判定。
- [x] 缺料/必要溶媒/容量超限/无实际有效药效拒绝开工且不消耗。无毒功能成分可用，纯辅助抵消粉不算独立药效。
- [x] 逐条映射相冲、溶媒不相容、助溶不足到消耗后药渣；不再输出沉淀注射液，不留注射后相冲致死分支继续处理这些新失败配方。
- [x] 合法高毒配方照常产药；成功配置不额外随机失败。制作失败不增加角色体内毒性/依赖。
- [x] 动态配方身份归并重复成分并排序，涵盖全部投入；名字、顺序不影响，剂量不约分；实例保留成分和规则版本。
- [x] 移除recordHistory/getHistory/getHistoryInputs、历史UI、回填、save字段与assess历史豁免。保留配方身份规范化的独立工具，不能因它以前叫historyKey就把身份能力一并删除。

涉及：js/pharmacy-compounding.js、js/pharmacy-station.js、js/pharmacy-station-panel.js、js/save-system.js、相关本地化字符串。当前recordHistory在applyInjection里调用、assess依赖history_conflicts，是明确旧入口。

## D. 药效来源、续药与同类最高值

- [x] 用固定成品ID或规范化动态配方键作为药物来源；模板ID不能代替来源。同一模板可被不同成品引用，不能因buff已存在跳过用药。
- [x] 同来源刷新结束时间，已起效不间断，等待中保留原起效时间，结束后重新等待。
- [x] 多来源各自计时，按具体效果选最高；新来源起效前不参与。各效果自己的持续时间独立，复方不能统一延长成最长项。
- [x] 生存恢复各字段分别选值，疲劳减少注意符号，倍率不连乘；不影响非制药增益。
- [x] 外敷按部位分组；现有按buffId保存目标的接口需核对是否只能保存一个部位，必要时改来源+目标表达。
- [x] 恢复预算和量化累计器不能让被压过来源补发，不能因续药刷新而追溯重放已经发过的恢复。药效总预算采用实际作用tick，与免疫和时长延长一致。

涉及：js/item-use.js（同buff跳过逻辑）、js/buff-system.js、js/pharmacy-effects.js（setTopicalTarget/tickPartRecovery）、js/survival.js、js/combat-enemies.js中相关消费者。优先在制药来源层合并，避免全局改变普通buff规则。

## E. 成瘾、毒性、清毒

- [x] 独立“成功使用结算”一次：消耗、依赖、口服毒性不由首次applyBuff事件决定；不会因重复药/被压过免除，也不重复扣账。
- [x] 成分级依赖增量与途径、免疫修正；缓解按最高已起效来源且与其主药效同步；功能成分不提供缓解。保持25/50/75阈值、既有五维惩罚和自然衰减。
- [x] 镇痛四丸和清毒使用已定数值；其余成分表以明确标记的可调首版候选接入，交付列出未逐项确认的数值，不伪称全部已确认。
- [x] 口服每次正毒性总和×0.2加入池；不要把上游消耗的所有原料重复当成实际摄入。清毒为0毒性、0依赖。
- [x] 清毒起效后额外0.75/tick，基准共2.25，40tick含3tick等待；归零后无储存，不直接减成瘾，不叠速。
- [x] 明确tick顺序并测试边界：自然与额外代谢、毒性档位、危险计数保持一致；重档>55，16tick。不能照搬文字估算宣称必能救回某初值。

涉及：js/pharmacy-effects.js、js/item-use.js、js/pharmacy-compounding.js、js/buff-system.js、js/scene-app.js、数据构建及成瘾参考生成器。

## F. 堆叠与腐败

- [x] 所有成品按medicine_stack_review逐项2～3，保留快捷格1；原料不跟着一律改成2～3。清毒已加入表。
- [x] 库存及仓库实例复制必须保留成分、版本、腐败进度；动态配方不兼容不得合并。
- [x] 所有成品基础7200；按实际消耗原料最低新鲜度比例继承。检查批量消耗不同年龄实例的行为，不只读取模板。
- [x] 合并取较短期限并提前提示；全非腐败输入视全新为技术默认。数量不能在合并与腐败处理中重复扣除。
- [x] 过期直接移除，只给当次玩家可见药名和数量提示；不转药渣、不新增变体。
- [x] 冷藏使用有效老化进度。当前仓库冷藏会停止仓内老化，不擅自重新设计为某个未知倍率；其他容器正常累计。

涉及：js/hideout-warehouse.js（tickInstanceSpoilage/resolveSpoiledInstance）、js/inventory-equipment.js（copyItemInstance与合并）、js/pharmacy-station.js、js/save-system.js、相应面板。虽不做旧档迁移，新系统正常保存恢复仍要可靠。

## G. 玩家界面与知识边界

- [x] 两入口保留：药粉与成品制作 / 注射液配置。普通制造显示实际成功率、成本、标准产量、推荐等级，不再显示“等级不足禁止”。
- [x] 配伍信息按0/1/2/4/6阶段过滤；0级可见现象不代表低级知道全部精确失败原因。已可判断必败则提示，不以等级替代配方判定。
- [x] 当次成功/失败/过期提示齐全；无历史图鉴、无重复失败提醒。
- [x] 用药前提示将刷新或已有更强效果，并显示已解锁的本次风险；不禁止合法但无即时增强的用药。
- [x] 状态栏分别显示依赖实际阶段与缓解后惩罚阶段，镇痛真实/有效疼痛继续保留；过期数量按件数而非仅格数报告。

## H. 构建与验收

先更新源与构建器，再生成，不手调生成输出。依赖顺序：

1. node tools/build-items-json.mjs
2. node tools/build-pharmacy-recipes.mjs
3. node tools/build-pharmacy-buffs.mjs
4. node tools/build-pharmacy-addiction-values.mjs
5. node tools/build-pharmacy-recipe-plan.mjs
6. node tools/export-pharmacy-reference.mjs

规划导出器现已允许new_products匹配运行时条目并标记“已实装/储备/暂缓”，不会双计数。成瘾参考生成器会显示当前41种药粉、28件固定成品和32个动态注射校验例，并明确候选数值状态。

现有测试入口（按新规则更新预期，不删除有效断言）：

- node tools/smoke-pain.mjs
- node tools/smoke-pharmacy.mjs
- node tools/test-pharmacy-assessment.mjs
- node tools/test-combat-engagement.mjs

新增或扩展针对性集成用例：固定/动态续药与逐次成本；同模板不同来源；不同部位；普通失败25%与配置失败0；配伍失败药渣且未入体；合法高毒成功；移除历史后信息不泄露；保存期限制作/合并/冷藏/过期；清毒实际危险tick顺序。保留原正常玩法和其他生活技能回归，不只镜像实现写单元测试。

本地游戏完成一条端到端路径：取得原料→粉碎→制丸→背包/快捷格→服用等待→续药→混用→到期；另一条：正确配置成功/错误配伍药渣/合法高毒及清毒。仅参考网页显示正确不等于游戏可用。

## 完成交付

- [x] 更新本清单每项实际状态，解释暂缓分支及原因。
- [x] 主文档更新实际已实现范围与当前启用数量，数值草案仍保留确认状态。
- [x] 报告构建、测试和游戏实测结果；未运行项明确写未运行。
- [x] 标出未确认的经济平衡候选（实际交易价、原料获得频率未审计完），不要把基础估值倒挂当作已证明套利。
- [x] 提交交付摘要供用户 review；不自动发布外网或宣称部署完成。

## 本轮落地结果

- 运行配方记录76条，启用69条；固定成品28件、药粉41件、制药源表物品96件。固定注射成品为0，注射统一使用动态配置实例。
- 暂缓7条消费者缺口分支：`life_pharmacy.macerate_cardiac_powder`、`life_pharmacy.crush_baiji_powder`、`life_pharmacy.crush_notoginseng_powder`、`life_pharmacy.refine_notoginseng`、`life_pharmacy.purify_notoginseng`、`life_pharmacy.blend_coagulant_powder`、`life_pharmacy.blend_coagulant_salve`。三条兽心强心替代线仍只在规划中。
- `tools/smoke-pharmacy.mjs` 104组、`tools/smoke-pain.mjs` 64组、制药信息分级、战斗接线、仓库腐败与全量UI窗口测试均通过。构建器六步和参考表重放验证通过。
- 本地游戏页加载正常且无控制台错误；最新参考页人工核对显示69条启用配方、28件固定成品、注射0件。当前测试存档的制药台尚未完成修复前置，因此完整制药操作链由真实运行模块集成测试覆盖，未把这部分记作手动存档实玩。
- 药材取得频率、商店成交价和成品套利空间仍属于后续经济平衡工作；本轮只落已确认配方成本和堆叠上限。除镇痛四丸及清毒外，成分级依赖数值仍为可调首版候选。
