# 57. 主角共用骨架棋子接入

> 已被用户纠正：本页把“认可骨架参数”误写为“认可校准示意人物外观”，该解释及外观接入已撤回。当前主角使用完整图册外观，见[58号修正记录](58-player-atlas-runtime.md)。以下保留为历史。

2026-09-14。用户确认56号校准预览“这套非常可以”，授权先将主角部署进游戏。

## 接入内容

- 主角使用 shared-humanoid-rig-v1，骨架长度、锚点及128状态矩阵保持不变。
- 主角素材位于 assets/map/isometric/player-rig-v1/D000.svg–D127.svg。由校准页同一 renderer.mjs 生成，无地块、骨架线或接触标记；保留简化灰色通勤造型、底座及随当前可见部件生成的短影。
- js/player-pawn-rig.js 读取 CharacterAttributes.getPartDestroy/getBodyPartDestroyMax，以现有系统的满值判定拼成七位状态。恢复后重新选图，没有自建阈值、永久残肢标记或游戏规则修改。
- 接在 SceneApp 的角色HUD更新中，仅在状态改变时请求新素材。新样式关闭旧棋子伪元素影子，保留地图位置、方向指示和饥饿提示。图片加载失败时实际运行版本可继续走原v4路径。
- NPC没有替换。新主角是用户确认的校准形象，并非此前128格生成图中的造型，截图不参与资产求解。

## 文件与验证

运行 node tools/build-player-pawn-assets.mjs 可从已保存的姿势矩阵生成128张矢量素材。manifest.json记录源姿势文件哈希和位序。校准页与实际素材共用renderer.mjs，避免两套绘制代码发生偏差。

node tools/test-player-pawn-rig.mjs [实际游戏目录] 使用真实CharacterAttributes模块检查128状态的阈值、恢复、旧加载回调防护、失败回退，以及全部素材不存在地块或调试骨架。骨架几何检查和地图投影检查也通过。

已在实际运行目录 C:/Users/haoxian.huang/.codex/worktrees/6254/SextingReborn2.0 的游戏页面查看：新主角站姿正常、底座对齐当前格、旧阴影未重复，方向与饥饿提示保留。数值验证采用隔离状态，未为测试修改玩家当前存档。当前没有新增行走动画、朝向骨架变化或姿势过渡动画。

本次主角接入是对旧v4静态主角默认基准的后续修订；已保存的设计数据基线保持原样。后续新增NPC继续按同一骨架数据复用，外观独立配置。
