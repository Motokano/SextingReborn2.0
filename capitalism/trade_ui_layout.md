# 交易界面布局说明 (trade_ui_layout.md)

本文档约定交易弹窗的布局与交互要素，供实现时参照。规则与设计意图以 `basic.md` 为准。

---

## 一、基本约定

- **无「持有银两」概念**：本作中物品本身即一般等价物，不以独立货币（如银两）展示。交易以物易物为核心。
- **主内容区**：两个列表左右并排，等宽各占一侧。

---

## 二、主内容区：双列表结构

- **左侧**：玩家持有且可供交易的物品列表。
- **右侧**：交易对象可供交易的物品列表。

两列等高、可独立滚动；空态显示「空」。

### 两侧行数不一致时

- 以**行数较多的一侧**为基准确定主体结构的行数。
- **行数较少的一侧**在不足部分**留空**（即多出的行不显示物品与操作列，仅保留空行或占位，使两侧行数对齐）。
- 这样表格总行数 = max(左侧物品数, 右侧物品数)，避免因两侧长度不同导致错位或布局异常。

### 双列表下方的感受行

- 在双列表表格**下方**隔开一行（留空或分隔线），单独显示一行文案：
  - **格式**：`你觉得 [感受词]，[交易对象] 觉得 [感受词]。`
  - **[交易对象]**：当前交易对象的称呼（如行商、老者），来自 NPC 的 `sn` 或商人配置的显示名。
- **感受词**：仅从以下 **5 种** 中根据实际交易情况**各选一个**显示（玩家侧一个、对方侧一个）：
  - **不行**
  - **略亏**
  - **可以**
  - **略赚**
  - **很赚**
- 感受词由双方**给出/得到的物品的基线价值**比较得出：基线价值来自物品表中的 `base_value` 字段，仅存于表与文档，**不在任何界面向玩家展示**；实现时据此计算后从上述 5 词中择一显示。

---

## 三、列表单行结构

每一行包含两大部分：

1. **物品与数量**：`[物品名] × 数量`
2. **操作列**（在数量右侧，单独一列）：
   - **步进区**（在数量的左侧）：`[ -10 ]`、`[ -1 ]`、当前数量 `[ N ]`、`[ +1 ]`、`[ +10 ]`
   - **动作区**：`[给 N 个]` / `[要 N 个]`、`[全都给]` / `[全都要]`；「给 N 个」与「全都给」（或「要 N 个」与「全都要」）之间留 **1 个半角空格** 的间距。

### 左侧（玩家可交易物品）

- 步进：`[ -10 ]`、`[ -1 ]`、`[ N ]`、`[ +1 ]`、`[ +10 ]` 用于修改「本次要给出的数量」。
- 按钮：`[给 N 个]` 按当前 N 执行给出；`[全都给]` 给出该物品全部持有量。

### 右侧（对方可交易物品）

- 步进：`[ -10 ]`、`[ -1 ]`、`[ N ]`、`[ +1 ]`、`[ +10 ]` 用于修改「本次要拿取的数量」。
- 按钮：`[要 N 个]` 按当前 N 执行拿取；`[全都要]` 拿取对方该物品全部数量。

### 步进与范围

- 步进单位：±1、±10（共四个按钮）。
- 当前数量 N 不得超过该行物品的可用数量（左侧不超过玩家持有，右侧不超过对方持有）。
- 实现时可将 N 夹在 0 与可用数量之间，或在边界处禁用对应的步进按钮。

### 步进按钮的交互效果

- 交互者点击**任意步进按钮**（`[ -10 ]`、`[ -1 ]`、`[ +1 ]`、`[ +10 ]`）时，**仅使页面上显示的该行「操作数量」N 发生变化**。
- **不**对库存进行任何实际增减或交换操作，**不**执行任何其他指令。步进只影响界面上的数值展示，真正改变库存的动作由「给 N 个」「要 N 个」「全都给」「全都要」或后续的结算流程触发。

---

## 四、布局模板（ASCII）

### 单行示例（左侧）

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [物品 A]  ×  3    │  [ -10 ]  [ -1 ]  [ 1 ]  [ +1 ]  [ +10 ]    [给 1 个] [全都给]  │
└─────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 单行示例（右侧）

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ [物品 X]  ×  5    │  [ -10 ]  [ -1 ]  [ 1 ]  [ +1 ]  [ +10 ]    [要 1 个] [全都要]  │
└─────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 两列完整表

```
  玩家可交易物品                                                                    对方可交易物品
┌──────────────────────────────────────────────────┬──────────────────────────────────────────────────┐
│ 物品A × 3   │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 物品X × 5   │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│ 物品B × 1   │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 物品Y × 2   │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│ 物品C × 12  │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 物品Z × 8   │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
└──────────────────────────────────────────────────┴──────────────────────────────────────────────────┘
```

### 交易示例A

场景：左侧（玩家 A）持有 3 种材料 + 1 种低级货币，右侧（交易对象 B）持有 4 种材料 + 同种货币，双方货币数量不同。物品名来自 `materials`（矿石类 sn_basic）与 `currency_base`（创世结晶）。总行数以多的一侧为准，共 5 行；左侧第 5 行仅货币一行，右侧 5 行满。

**物品代号与名称对应**

| 代号 | 物品名（来源） |
|------|----------------|
| a | 红铁块 (iron_ore) |
| b | 黑铁石 (black_iron_ore) |
| c | 黄铜石 (yellow_copper_ore) |
| d | 锡亮石 (tin_shiny_ore) |
| e | 灰石块 (lime_stone_ore) |
| 货币 | 创世结晶（currency_base 低级货币） |

**布局示意**

```
  玩家可交易物品                                                    对方可交易物品
┌──────────────────────────────────────────────────┬──────────────────────────────────────────────────┐
│ 红铁块 × 5    │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 红铁块 × 8    │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│ 黑铁石 × 12   │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 黑铁石 × 2    │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│ 黄铜石 × 3    │ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 锡亮石 × 15   │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│ 创世结晶 × 140│ [-10][-1] [ 1 ] [+1][+10]  [给1个] [全都给] │ 灰石块 × 7    │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
│                │                                            │ 创世结晶 × 60 │ [-10][-1] [ 1 ] [+1][+10]  [要1个] [全都要] │
└──────────────────────────────────────────────────┴──────────────────────────────────────────────────┘
```

**数量汇总**

| 侧 | 物品 | 数量 |
|----|------|------|
| A（玩家） | 红铁块 | 5 |
| | 黑铁石 | 12 |
| | 黄铜石 | 3 |
| | 创世结晶 | 140 |
| B（对方） | 红铁块 | 8 |
| | 黑铁石 | 2 |
| | 锡亮石 | 15 |
| | 灰石块 | 7 |
| | 创世结晶 | 60 |

---

## 五、与设计文档的对应

- 交易上下文、结算时机、会话内未结算即关闭等规则见 `basic.md` 第 9 章。
- 商人配置与字段见 `trader_template.md`，物品说明与展示见 `items_template_and_style.md`。
- 本文件仅约定交易界面的布局与上述交互要素，其余（标题区、页脚、结算/关闭、以物易物时的模糊暗示等）可在此基础上扩展，或在与 `basic.md` 一致的前提下另行约定。
