---
description: 速度先手、同速同时提交、玩家发起交换与敌人还击 — 实现与数据字段索引，供改 scene-app / 管线 / 敌人表 / 近战 resolve 时遵循
globs: "**/js/combat-initiative.js,**/js/combat-melee-resolve.js,**/js/combat-pipeline.js,**/js/combat-enemies.js,**/js/scene-app.js,**/data/combat-enemies.json,**/data/combat-pipeline.json,**/index.html,**/docs/design/07-combat-core.md,**/docs/design/14-implementation.md,**/.cursor/rules/game-design-reuse-contract-from-docs.mdc"
alwaysApply: false
---

# 先手 / 同速交换与敌人还击（Agent 规则）

策划语义以 **`docs/design/07-combat-core.md`**「速度与先手、连击」为准；**取整战斗速度**公式见 **`docs/design/05-character-attributes.md`** §5.7。与 Buff、后遗症的分工：**命中/试探/驱散** 仍遵守 **`buff-system-agent.mdc`**、**`post-effects-agent.mdc`**；本规则只管**交换顺序、管线 dry-run、敌人还击数据字段**。

**全局合约摘要**：**`game-design-reuse-contract-from-docs.mdc`** §8.0（速度先手与同速同时提交）。

## 单一事实源（实现）

| 职责 | 位置 |
|------|------|
| 先后手判定（速度 + `initiative_always_first` 互抵） | **`js/combat-initiative.js`** → `CombatInitiative.resolvePlayerInitiatedExchange` |
| 槽位后遗症 id（与 UI 装配一致） | **`CombatInitiative.getPostEffectIdsForMoveSlot`**（读 `InventoryEquipment.getCombatState()`） |
| 玩家攻敌 resolve | **`CombatMeleeResolve.resolvePlayerVsEnemyAttack`**；同速双演算时 **`deferResourceSpend: true`** |
| 敌人还击 resolve | **`CombatMeleeResolve.resolveEnemyVsPlayerAttack`**；扣费提交用 **`applyDeferredResourceSpendFromResolveResult`** |
| 同速「延迟提交」管线 | **`ctx.simultaneousDryRun`**；**`CombatPipeline.flushPendingBuffApplies`**、**`finalizeSimultaneousStrike`** |
| 玩家发起交换编排 | **`js/scene-app.js`** → **`SceneCtx.actions.attackEnemy`** |
| 敌人模板合并到 `ctx.defender` | **`CombatEnemies.mergeIntoDefender`**（**`data/combat-enemies.json`**） |

## `index.html` 脚本顺序（强制）

**`combat-post-effects.js`** → **`combat-initiative.js`** → **`combat-pipeline.js`** → **`combat-melee-resolve.js`**（`resolveEnemyVsPlayerAttack` 依赖 `CombatInitiative` 的占位 skill/move id）。

## `CombatInitiative` 行为要点

- **输入**：`playerSpeed`、`enemySpeed`（**已与 `CharacterAttributes.getCombatSpeed()` / 敌人表 `speed` 对齐的取整值**）、`attackerPostEffectIds`、`defenderPostEffectIds`（可选）、`skillId`、`moveId`。
- **`initiative_always_first`**：仅当 `CombatPostEffects.validateSocket(pe, { skillId, moveId })` 通过且 `effect_type === 'initiative_always_first'` 时视为攻击方强制先手；防守方列表来自敌人 **`counter_post_effect_ids`**（见下）或将来扩展。
- **双方均强制先手**：互抵后回退纯速度比较。
- **输出**：`mode: 'sequential' | 'simultaneous'`；`firstStrike: 'player' | 'enemy'`（同速时 `firstStrike` 仅为占位，实际走同时提交）。
- **敌人还击占位 id**：`getEnemyAttackSkillId()` → **`__enemy_counter_attack__`**，`getEnemyAttackMoveId()` → **`enemy_counter_strike`**（**勿**在 `combat-skills.json` 与玩家技能混用同名业务含义，除非你有意统一表）。

## `attackEnemy` 分支（实现口径）

- **`can_attack === false`**（或未配置且默认可还击时仍由表控制）：**不**执行敌人还击；同速时**不会**进入 `simultaneous` 双端 dry-run（退化为仅玩家一侧）。
- **顺序 · 玩家先手**：`resolvePlayerVsEnemyAttack`（正常扣费）→ **`melee_hit_enemy_defender`** → 若可还击 → `resolveEnemyVsPlayerAttack` → **`melee_hit_player_defender`**。
- **顺序 · 敌人先手**：先还击管线，再玩家 resolve + **`melee_hit_enemy_defender`**。
- **同速且可还击**：两侧 resolve（玩家 **`deferResourceSpend: true`**）→ 两侧管线均设 **`simultaneousDryRun: true`** → **`flushPendingBuffApplies`（先玩家 ctx 再敌人 ctx）** → **`applyDeferredResourceSpendFromResolveResult`** → **`finalizeSimultaneousStrike`（先玩家再敌人）**。

**熟练度**：仍以**玩家**攻击上下文上的 **`proficiencyDelta`** 为准，在交换末尾 **`InventoryEquipment.adjustSkillMoveUsage`** 一次。

## `ctx.simultaneousDryRun`（`combat-pipeline.js`）

为 true 时：

- **`builtin.apply_move_hit_roll_buffs`**：不直接 `applyBuff`，追加到 **`ctx.pendingBuffApplies[]`**（`{ owner, buffId, src, evCtx }`）。
- **`builtin.post_effects_hook`**：**不执行**（避免与 commit 重复）；commit 时由 **`finalizeSimultaneousStrike`** 内 **`runPostEffectsForHook(..., 'hit_roll_success')`** 补跑。
- **招架 / 命中 roll / 护体吸收 / 伤害** 中带 **`emitCombat`** 的阶段：**不发射**（避免 Buff 管线双触发）。
- **`builtin.damage_stub`**：写入 **`ctx.simultaneousPendingDamage`**（含 `defenderKind`、`finalDamage`、`ctxRef`），**不**调用 **`onEnemyDamageResolved`**。

**`finalizeSimultaneousStrike`**：后遗症 → 若防方为 enemy 则 **`onEnemyDamageResolved`** → 再 **`BuffSystem.triggerBuffPipeline`** 补发 **`attack_hit_roll_resolved`** / **`attack_damage_applied`**（tags 与常规管线一致，**勿**随意新增未注册 tag）。

## `data/combat-enemies.json` 字段（还击 / 先手扩展）

| 字段 | 说明 |
|------|------|
| **`speed`** | 整数，参与先后手与命中比（与玩家 `getCombatSpeed()` 同口径比较）。 |
| **`can_attack`** | `false` 时**不**还击、不进入同速双端同时提交。 |
| **`attack_damage_min` / `attack_damage_max`** | 还击原始伤害区间（闭区间整数采样）；缺省由 **`resolveEnemyVsPlayerAttack`** 内回退 **6～14**。 |
| **`attack_damage_type`** | 还击伤害类型，默认 **`blunt`**。 |
| **`counter_post_effect_ids`** | 可选字符串数组：防守方 **`initiative_always_first`** 等后遗症 id（**稀有**；需能在 `post-effects.json` 查到且语义符合 socket）。 |

**`CombatEnemies.mergeIntoDefender`** 会把上述字段写入 **`ctx.defender`**（供 `attackEnemy` 读取 `counter_post_effect_ids`）。

## 与 `game-design-reuse-contract` 的关系

- §8.0 描述**玩家可见规则**；**落地细节与文件索引**以**本 mdc** 为准。
- 修改 §8.0 文案时若涉及实现行为，**同步**检查本文件与 **`07-combat-core.md`** 是否一致。

## 已知缺口（后续 agent 可接单）

- **后手短路**：先手结算后若敌人**已死亡/离场**，应**跳过**还击；待敌人 HP 与地图实体生命周期接入后在 **`attackEnemy`** 或统一 **`CombatEnemies`** API 补判。
- **连击多段**：`07` 中速度倍数连击尚未在 **`attackEnemy`** 拆多段先后手；扩展时勿绕过 **`InventoryEquipment`** 槽位校验。
- **同速 dry-run**：若未来在 dry 阶段依赖更多「立即生效」的副作用，需逐项评估是否应并入 **`pending*`** 或 commit 顺序。

## 勿混用

- **步法 / 战斗速度公式**、命中率曲线：仍以 **`05`**、**`08`**、**`survival-config.json`** 的 **`hit_*`** 为准；本规则**不**重复定义数值公式。
- **击退 tick 末坍缩**：仍 **`07`** + **`combat-pipeline`** 既有击退逻辑；与先手交换**独立**（除非单条设计声明绑定）。
