---
description: 基地仓库：调试无限取物 vs 藏身处账号仓 hideout_warehouse；NPC 入口与存档口径
globs: js/game-engine.js,data/maps/M0_Base_Inside_lv_1.json,js/scene-renderer.js,js/scene-app.js,index.html,data/ui_text_zhCN.json,js/inventory-equipment.js,js/hideout-warehouse.js,data/warehouse-upgrades.json,warehouse-upgrade-roadmap.html,data/npc/**
alwaysApply: false
---

# 基地仓库 Agent 规则

本作有两套「仓库」相关入口，**禁止混用 API 与存档**：

| 类型 | 用途 | 状态 |
|------|------|------|
| **调试仓** `modal-base-warehouse` | 无限从 `items.json` 取 1 | **过渡期保留** |
| **账号仓** `hideout_warehouse` | 真库存、升级、腐败/冷藏 | **设计定案，待/已接实现** |

设计正本：**`docs/design/29-hideout-warehouse.md`**。升级表：**`data/warehouse-upgrades.json`**。

---

## 1) hideout_warehouse（账号仓，强制）

- **存档字段**：`hideout_warehouse`（绑定**当前唯一 save slot**）；与 `InventoryEquipment`、世界 Storage **分离**。
- **死亡**：仓内容 **不清**（见 `03-death-and-insurance.md`）。
- **容量**：限格不限重；基础 **100 格**；**不做**负重 \(W\) 检查。
- **堆叠**：模板 **`stack_limit > 1`** 默认可仓内叠 `count`；**`deposit_auto_stack`** 为 **开局免费**。
- **实例**：完整物品实例（装备、`quality_tier`、未鉴定状态等）原样存取。
- **展示**：`getDisplayName` / `getDisplayDesc` 须传 **`character`**。
- **入口**：**设施 NPC**（`warehouse_station_interact_npc_*` + `interactNpc` + 主菜单开面板）；**非**默认直开 modal。
- **A0**：开局剧情 **flag** 解锁正式开仓（flag id 由剧情配置；`warehouse-upgrades.json` 中 `requires` 含 `A0`）。
- **开局免费（A0 后）**：100 格、四容器手动存、`deposit_auto_stack`、取出按 05 §5.5.5 放入顺序。
- **升级**：仓库面板内「扩建」；扣 `warehouse-upgrades.json` 的 `inputs` + `task_ticks` / `stamina_per_tick`。
- **扩建计时**：仅扩建子层打开时按 `panel_tick_ms`（默认 2000ms）推进并扣体力；关面板/关子层暂停；施工中禁止移动。
- **路线发现**：首档 **三选一**（U-A1 / U-C1 / U-B1）；`discovered_upgrade_ids` 外节点 **完全隐藏**；完工后按 `requires` 全满足自动发现下一环。
- **U-G2 远驿**：当次地牢有效；**能存不能取**。
- **U-G3 冷藏**：整仓冻结 `spoilage_elapsed_ticks`；关游戏全局冻结 tick。
- **U-F2 工料仓**：设施扣料可扫仓；玩家可 **优先扣仓**（`prefer_deduct_warehouse`）。
- **运行时（建议）**：`js/hideout-warehouse.js` 为读写唯一入口。

---

## 2) 调试仓（无限取物，过渡期）

- 地图 `annotations["x,y"] === "仓库"`（如 `M0_Base_Inside_lv_1.json`）。
- 动作栏：`action-bar-warehouse`（`scene-renderer.js`）；文案 `action.bar.warehouse`。
- Modal：`modal-base-warehouse`；列表 `InventoryEquipment.getAllItemIds()`；取 1 走 `putItemIntoDefaultContainer`，**不扣仓库存**。
- 与 **`hideout_warehouse`** 并存时 UI/文案须区分「调试取物」与「账号仓库」。

---

## 3) NPC 设施台约定（新仓）

- 地图绑定：`warehouse_station_interact_npc_by_cell["x,y"]`（或回退 `warehouse_station_interact_npc_id`）。
- 有绑定 NPC 时邻格优先 **`interactNpc`**；设施 NPC `tags` 建议 `warehouse_station`；主菜单 `showOpenHideoutWarehousePanel`（或等价）。
- 注册表路径同灶台/制肥桶：`def` + `triggers` + `dialogue_pools` + `npc_registry.json`。

---

## 4) 农业扣物

- `AgriculturePlayerItems` 枚举须含 **`hideout_warehouse`**；优先扣仓由玩家设置控制。见 `28-agriculture-irrigation.md` §16.3。

---

## 5) 禁止事项

- 禁止用调试仓逻辑冒充 `hideout_warehouse` 存档。
- 禁止仓与世界箱/载具柜共用同一 `slots` 结构而不分字段。
- 禁止地牢远驿开放 **取出**（仅 U-G2 当次地牢 **存入**）。
