---
description: 给外部 UI agent 生成设计 prompt 时，强制要求与现有背包 UI、战斗技能 UI 的主题色调与视觉语气一致
alwaysApply: true
---

# UI Agent Prompt 主题一致性规则

当需要为“不能读取仓库的外部 UI agent”撰写 prompt 时，必须包含以下硬约束：

1. **主题色调一致（强制）**
   - 明确写入：新 UI 必须与现有**背包 UI**与**战斗技能 UI**保持一致的主题色调、明暗层级、对比关系与信息密度风格。
   - 禁止出现“另起一套全新配色宇宙”的表述。

2. **视觉语气一致（强制）**
   - 文本优先、信息优先、功能可读性优先。
   - 不使用过度花哨、强装饰、赛博霓虹等与现有系统冲突的视觉方向。

3. **在 prompt 中必须出现的字面要求**
   - “与背包 UI、战斗技能 UI 主题色调保持一致”
   - “延续现有 UI 的文本呈现为主的风格”

4. **默认输出要求（建议）**
   - 让 UI agent 输出“主题对齐点清单”（至少 6 条），逐条说明如何与背包/战斗技能 UI 对齐。
   - 若 UI agent提出新颜色，必须同时给出“与现有主题映射关系”（主色/辅色/警示色/禁用色）。

