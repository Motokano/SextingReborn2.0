---
description: Cooking migration into unified recipe system
alwaysApply: true
---

# 烹饪迁移到统一配方路由（Agent 规则）

编辑烹饪配方匹配、烹饪制作流程、烹饪存档字段与路由接线时，必须遵守本规则，确保“迁移不改玩法口径”。

## 1) 迁移目标（强制）

- 将烹饪作为首个系统接入统一配方路由。
- 加载顺序：新配方表优先；旧烹饪表仅兜底。
- 首版仅单次制作，不做批量制作。

## 2) 行为保持（强制）

- 不得私改现有烹饪平衡口径：
  - 成功率计算路径
  - 熟练度增长时机
- 继续复用已有成功率计算模块与全局参数口径。

## 3) 匹配与结算（强制）

- 输入匹配允许超集投料。
- 开始制作即消耗全部投入材料（不是仅消耗命中 recipe 所需）。
- 制作开始即消耗时间（`ticks`）与成本。
- 命中多条配方：`match_weight` 加权随机；缺省权重=1。
- 成功时：
  - 固定发放 `main_output`
  - 对 `bonus_outputs` 逐条独立判定概率并发放
- 失败时：
  - 失败产物优先级：`recipe.failure_output` > `method.failure_output` > 全局失败产物

## 4) 成长与图鉴（强制）

- 熟练度计数仅在成功制作后增加。
- 已知配方仅在首次成功制作后记录。
- 存档双写兼容期：
  - 旧字段：`known_cooking_recipe_ids`
  - 新字段：`known_recipe_ids_by_system.cooking`

## 5) 占位条目与可见性

- `enabled:false` 的烹饪占位 recipe：
  - 编辑器可见
  - 运行时不可匹配

## 6) 路由与处理器注册（强制）

- `recipe_processor_id` 必须来自集中注册表。
- 未注册处理器不得静默 fallback 到任意默认实现，必须报错并给出可定位信息。

## 7) 回归验证最小清单

- 新表可用时，烹饪可完整制作（匹配、消耗、成功/失败、产出、熟练度）。
- 新表损坏时，旧表兜底仍可继续烹饪。
- 旧存档读入后，烹饪图鉴与新字段同步写入正常。

## 8) 禁止事项

- 禁止移除旧烹饪字段兼容（未到收口阶段）。
- 禁止将“非法新表”当作静默空表处理（必须报错+可见）。
- 禁止引入与统一配方路由并行的第二套烹饪新入口。
